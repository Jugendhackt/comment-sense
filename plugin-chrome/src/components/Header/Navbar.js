import React from "react";
import { AppBar } from "@material-ui/core";

const Navbar = (props) => {
    return (
        <AppBar position="static">
            
        </AppBar>
    );
};

export {Navbar};